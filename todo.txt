* integrate with clientcide lib:

	if (window.Depender && Depender) Depender.loaded.... //nix loaded reference on instance; make shared

* split Depender Class into server and clientcide versions
* reconsider using a Class
* save/load a state? (name it?)
* clear cache method that empties outputs/
* ensure that headers allow for browsers to cache scripts